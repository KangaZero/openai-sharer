{
  "name": "openai-sharer",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.ts",
  "scripts": {
    "start": "node server/npm run compile && node ./dist/index.js",
    "dev": "concurrently \"cd server && npm run dev-server\" \"cd client && npm run dev\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd server && npm run seed && cd ../client && npm run build",
    "compile": "cd server & npm run compile"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
